import { Particle } from "../Particle";

export function spawnParticle(  
    grid: (Particle | null)[][],
    x: number,
    y: number,
    particle: Particle
): boolean {
    if (x >= 0 && x < gri)
}

}